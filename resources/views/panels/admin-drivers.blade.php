@extends('layouts.theme')

@section('title', 'Driver Applications | Nogaru Car Rental')
@section('page-title', 'Driver Applications')
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card"><div class="card-header"><h4 class="header-title mb-1">Review driver applicants</h4><p class="text-muted mb-0">Only approved drivers appear in business hiring lists.</p></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Driver</th><th>License</th><th>Experience</th><th>Documents</th><th>Status</th><th></th></tr></thead><tbody>@forelse($drivers as $driver)<tr><td><strong>{{ $driver->full_name }}</strong><small class="d-block text-muted">{{ $driver->email }} · {{ $driver->phone }}</small><small class="d-block text-muted">{{ $driver->city }}</small></td><td>{{ $driver->license_number }}<small class="d-block text-muted">{{ $driver->license_type }} · expires {{ $driver->license_expires_at?->format('M d, Y') }}</small></td><td>{{ $driver->years_driving_experience }} years total<br><small class="text-muted">{{ $driver->professional_driving_experience }} professional</small></td><td><a href="{{ asset('storage/'.$driver->license_front_path) }}" target="_blank" class="btn btn-sm btn-outline-secondary">Front</a> <a href="{{ asset('storage/'.$driver->license_back_path) }}" target="_blank" class="btn btn-sm btn-outline-secondary">Back</a></td><td><span class="badge bg-{{ $driver->approval_status === 'approved' ? 'success' : ($driver->approval_status === 'pending' ? 'warning' : 'danger') }}-subtle text-{{ $driver->approval_status === 'approved' ? 'success' : ($driver->approval_status === 'pending' ? 'warning' : 'danger') }}">{{ ucfirst($driver->approval_status) }}</span>@if($driver->rejection_reason)<small class="d-block text-danger">{{ $driver->rejection_reason }}</small>@endif</td><td class="text-end text-nowrap">@if($driver->approval_status !== 'approved')<form class="d-inline" method="POST" action="{{ route('admin.drivers.approve', $driver) }}">@csrf @method('PUT')<button class="btn btn-sm btn-success">Approve</button></form>@endif @if($driver->approval_status !== 'rejected')<form class="d-inline" method="POST" action="{{ route('admin.drivers.reject', $driver) }}" onsubmit="return confirm('Reject this driver application?')">@csrf @method('PUT')<input type="hidden" name="rejection_reason" value="Requires further verification."><button class="btn btn-sm btn-outline-danger">Reject</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">No driver applications yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
